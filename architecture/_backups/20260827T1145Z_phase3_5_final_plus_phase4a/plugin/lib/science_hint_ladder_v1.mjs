import { classifyScienceSubskill } from "./science_subskill_map.mjs";
const LABELS=["不需要提示","回憶核心概念","拆解判斷步驟","提供部分示範","完整模型與檢核"];
export const SCIENCE_HINT_LEVELS=LABELS;
export function nextScienceHint(input={}){const attempts=Math.max(1,Number(input.attempts)||1), codes=Array.isArray(input.error_codes)?input.error_codes:[]; const primary=codes[0]?.startsWith("SCI-METH")?"experiment":codes[0]?.startsWith("SCI-DATA")?"data_interpretation":codes[0]?.startsWith("SCI-DIAG")?"diagram":classifyScienceSubskill({knowledge_point:input.knowledge_point||""}).primary_subskill; let level=attempts>=5?4:attempts>=3?3:attempts>=2?2:1; const reps={concept:"diagram",causal_reasoning:"diagram",experiment:"experiment",data_interpretation:"table",diagram:"diagram",safety:"text",vocabulary:"text"}; const representation=input.representation==="text"?"text":primary==="experiment"?"experiment":primary==="data_interpretation"?"table":reps[primary]||"text"; const kp=input.knowledge_point||"這個科學概念"; const texts={1:`想一想「${kp}」中最重要的科學關係是什麼？`,2:"先找出已知條件，再逐步判斷每一個變因或圖表訊息。",3:"可以先用一個相近的簡單例子，套用相同規則，再回到原題。",4:"模型解法：先確認概念與條件，再依序套用規則，最後用單位、趨勢或實驗結果檢查答案。",0:"答對了，繼續保持。"}; return {level,level_name:LABELS[level],hint_text_zh:texts[level],representation_suggestion:representation,primary_error_code:codes[0]||null,reason:`${primary}-attempt-${attempts}`};}
